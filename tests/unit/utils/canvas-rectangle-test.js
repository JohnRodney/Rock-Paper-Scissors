import canvasRectangle from '../../../utils/canvas-rectangle';
import { module, test } from 'qunit';

module('canvasRectangle');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = canvasRectangle();
  assert.ok(result);
});
